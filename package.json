{
  "name": "qq-ai-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "QQ official bot on Cloudflare Workers + Durable Objects + D1 + DeepSeek",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GreyDust-114/cf-qq-bot.git"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.136.1"
  }
}
