# 生产灰度验收记录（2026-09-25）

## 范围

- Worker：`qq-ai-bot`
- 生产版本：`daf71726-8486-43b8-8940-9c619937fb24`（提交 `2519f9b`）
- 变更内容：角色资料库注入（D1 `lore` 6 条 / 18367 字符）与总字数硬预算 30 → 40（版本 `1cd9ac92`），提示词四层重构（版本 `daf71726`，提交 `2519f9b`）；观测窗口覆盖两者的最终状态
- 灰度群：由 `ALLOWED_GROUP_OPENID` 限定；窗口内只有 1 个群会话，无单聊
- 观测窗口：2026-09-23 11:48:00～2026-09-25 11:15:00 UTC（约 47.5 小时）
- 数据来源：远程 D1 `messages` / `outbox` 聚合查询、提示词评测报告（本地 `review/eval/20260923T114325-refactor.md`，不随仓库发布）

本记录只保存聚合数据，不包含真实消息正文。

范围说明：本记录验收 BOT-015 自身的验收标准（格式、长度、发送、资料库卫生与设定准确性）。2026-09-25 用户提供的账单还暴露了输入 token 成本偏高的问题，初步定位为分钟粒度的时间上下文插在静态块之前、打断前缀缓存；该问题不属于本任务验收标准，已由独立提交 `78ec932` 处理（消息顺序重排与 `stage=usage` 用量日志，机制见 `docs/behavior.md`、`docs/operations.md`），不在本记录中判定。

## 验收结论

| 验收项 | 证据 | 结论 |
|---|---|---|
| 设定问答不再编造、符合新约时间点 | 评测 37 场景全部成功，事实点（蕾缪安为第七厅枢机、162cm、苹果派物流、律法/灾异、罗德岛身份）全部正确；线上 223 条机器人消息中旧错误值 `159` 出现 0 次 | 通过（线上真实设定提问样本少，见「局限」） |
| 日常闲聊保持短句节奏 | 223 条气泡：平均 8.7 字 / 中位 9 / P90 12 / 最大 13；单轮中位 11 字 / P90 14 / 最大 25；换行 0 | 通过 |
| 皮肤与平行设定不算作现状 | 评测 `skin-boundary` 场景正确区分时装与正史；线上窗口内无皮肤相关提问 | 通过（以评测为准） |
| 本地语料不进入公开仓库 | 语料位于 `local/corpus/`（gitignored），公开树与 `0004_lore.sql` 只有表结构；运行时文本来自远程 D1 | 通过 |
| 灰度无格式与发送回归 | sent 223 / failed 0 / uncertain 0；时间前缀 0、气泡内换行 0；与 2026-09-23 记录相比长度与条数分布一致 | 通过 |

## 消息形态

### 机器人气泡

| 指标 | 平均 | 中位数 | P90 | 最大值 |
|---|---:|---:|---:|---:|
| 单条气泡字数 | 8.7 | 9 | 12 | 13 |
| 单轮回复总字数 | 10.9 | 11 | 14 | 25 |

条数分布（234 个有 outbox 的批次）：

- 1 条：176（75.2%）
- 2 条：54（23.1%）
- 3 条：4（1.7%）
- 4 条：0

与 2026-09-23 记录（单条中位 8、单轮中位 11、1 条占 69.1%）一致，没有因资料库注入变长或变啰嗦。

### 人类参考样本

窗口内 643 条群友消息：单条中位数 7 字、平均 13.5 字（长消息拉高均值），机器人气泡中位数 9 字，处于同一量级。

## 发送与 outbox

- outbox：296 行 / 234 批；sent 223、pending 73、failed 0、uncertain 0。
- assistant 记录数 223 与 sent 行数完全一致，无孤立记录。
- 73 条 pending 的构成：part 1 共 52 条（整批在发送前被更新消息抢占）、part 2 共 19 条、part 3 共 2 条（发送中途被抢占或批次预算不足而停止）。
- 52 个 0-sent 批次全部在 5 分钟内有后续消息进入同一会话，符合「新 revision 接管、丢弃过时回复」的设计路径；没有停滞批次。
- 时间前缀、气泡内换行、悬空分隔标点均为 0。

## 资料库使用情况

- 远程 D1 `lore` 6 条 / 18367 字符，与本地同步结果一致。
- 线上窗口内机器人消息命中「苹果派」2 次（均为角色风格的闲聊提及，未出现否认苹果派物流的旧错误）；「厘米」「162」「蕾缪安」出现 0 次。
- 群友侧命中：拉特兰 1 条、企鹅物流/苹果派 4 条；身高/厘米、蕾缪安、皮肤/时装均为 0 条。

## 局限与未验证项

1. 线上真实设定提问样本极少（见上），人设与世界观准确性主要由评测承担；灰度只能确认旧事实错误没有复现。
2. `plain-text-fallback` 与 `trimmed-total` 只写入 Worker 日志，本次未能采集窗口内日志（非 TTY 下 `wrangler tail` 无输出），以评测结果为准：回退 2/37、trimmed 1、前缀缓存 98%、延迟中位 1.36s。
3. pending 比例高于 2026-09-23 记录，但 failed / uncertain 为 0 且停滞批次为 0；若后续持续升高，按「运维判定」第 4 条检查批次是否有对应的 `batch done` 记录。
4. 本记录不覆盖成本与缓存命中率（按任务标准不计入验收）；该问题已由独立提交 `78ec932` 处理（消息顺序与前缀缓存、`stage=usage` 日志）。

## 运维判定

沿用 2026-09-23 记录的告警项，另增加：

7. `plain-text-fallback` 长期高频（评测为 2/37），说明模型持续偏离输出协议；
8. 机器人消息重新出现旧事实（如 `159`）或资料库与提示词再次冲突。

## 回滚

```powershell
npx wrangler rollback
```

回滚代码不会撤销远程 D1 `lore` 表：`loadLore` 在表缺失或为空时按「没有资料」运行，回滚旧版本不受影响；需要撤销数据时删除 `lore` 行即可，不修改历史迁移。
